# Google-File-Transfer-UIX

This project provides a simple Next.js dashboard to manage Google Drive file transfers. The current implementation contains placeholder pages that will be extended to support portal management, Google authentication and scheduled transfers.

## Getting Started

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm start
```
